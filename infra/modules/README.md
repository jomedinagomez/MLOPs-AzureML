# Terraform Modules

Reusable infrastructure modules supporting the Azure ML platform lie in this folder. Each child directory deploys a scoped resource (for example, registry, vnet, key vault). Reference these modules from `infra/main.tf` or compose them in other stacks as needed.
