# Data Assets

The `data/` tree carries sample datasets and configuration for Azure ML jobs.

- `taxi-data/` bundles YAML descriptors used to register sample taxi fare datasets.
- Never commit sensitive production data; use this area only for synthetic or publicly shareable assets.
